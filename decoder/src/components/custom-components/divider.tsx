import { FC } from "react";
import "styles/components.css";

const Divider: FC = () => (
  <section className="relative flex py-3 items-center">
    <div className="flex-grow border-t border-gray-400" />
  </section>
);

export default Divider;
